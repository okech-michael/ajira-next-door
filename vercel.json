{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist/client",
  "rewrites": [
    {
      "source": "/:path((?!api|.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}
